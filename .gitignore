# Build artifacts
/target/

# Runtime-generated data must not be versioned.
onyx.snapshot
onyx.snapshot.previous
onyx.binlog
onyx.replica
onyx.log
*.tmp

# The application dependency lockfile is versioned for reproducible builds.
